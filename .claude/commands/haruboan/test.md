---
description: 린트 및 타입 체크 실행
allowed-tools: Bash(npm run lint:*), Bash(npm run build:*)
---

1. `npm run lint` 를 실행하고, 에러가 있으면 첫 번째 에러 원인을 한 단락으로 요약해.
2. `npm run build` 로 TypeScript 타입 에러를 확인하고, 에러가 있으면 요약해.
